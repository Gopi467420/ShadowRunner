using UnityEngine;

public class GatherablePlank : Gatherable
{
    public GatherablePlank(string _type) : base(_type)
    {
        type = "Plank";
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
