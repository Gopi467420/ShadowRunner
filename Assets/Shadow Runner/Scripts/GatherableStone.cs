using UnityEngine;

public class GatherableStone : Gatherable
{
    public GatherableStone(string _type) : base(_type)
    {
        type = "Stone";
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
